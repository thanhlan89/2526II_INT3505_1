# OpenAPI Demo

## Cách cài đặt và chạy
1. Cài đặt Swagger CLI qua npm:
   `npm install -g swagger-cli`
2. Validate file YAML để kiểm tra cú pháp:
   `swagger-cli validate openapi.yaml`
3. (Tùy chọn) Copy nội dung `openapi.yaml` dán vào https://editor.swagger.io/ để xem UI.