# API Blueprint Demo

## Cách cài đặt và chạy
1. Cài đặt công cụ Aglio để render HTML:
   `npm install -g aglio`
2. Sinh file HTML từ Blueprint:
   `aglio -i api.apib -o index.html`
3. Mở file `index.html` bằng trình duyệt để xem kết quả.